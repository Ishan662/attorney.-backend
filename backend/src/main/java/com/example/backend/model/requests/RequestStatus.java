package com.example.backend.model.requests;

public enum RequestStatus {
        PENDING,
        ACCEPTED,
        SCHEDULED,
        RESCHEDULED,
        REJECTED,
        CANCELLED,
        COMPLETED
}